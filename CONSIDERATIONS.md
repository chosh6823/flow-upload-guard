# 고려사항

> 과제 3번 항목에 대한 판단과 근거입니다.
> **구현한 것뿐 아니라 "고려했으나 하지 않은 것"과 그 이유**를 함께 적었습니다.
> 각 항목은 `판단 → 근거 → (해당 시) 코드 위치` 순서입니다.

---

## 0. 먼저, 이 기능의 성격에 대해

이 과제를 처음 읽고 가장 먼저 든 생각은 **"확장자 차단은 원리적으로 불완전한 방어"** 라는 점이었습니다.
그래서 이 문서 전체를 관통하는 질문을 세 개로 정리하고 시작했습니다.

1. **불완전한 걸 알면서 왜 이 방식을 쓰는가** — §1
2. **불완전한 방어를 어디까지 보완할 것인가** — §3, §4
3. **그래도 뚫렸을 때 피해가 나지 않게 하려면** — §6

이 세 질문에 답하지 못하면, 만들어진 건 "정책은 있는데 실제로는 못 막는 화면"이 됩니다.

---

## 1. 정책 모델 — blocklist 인가 allowlist 인가

### 판단
**과제 명세대로 blocklist 를 구현하되, 그것이 차선책임을 명시하고 다층 방어로 보완한다.**

### 근거

보안 실무의 정답은 allowlist(허용 목록)입니다. blocklist 는 **"내가 모르는 위험한 확장자"를 원리적으로 막지 못하기** 때문입니다.
고정 7종에 커스텀 200개를 다 채워도 이런 것들이 남습니다.

```
msi  vbs  ps1  psm1  jar  hta  lnk  reg  wsf  wsh  scf  inf  dll  sys
jse  vbe  chm  cpl  msc  gadget  application  pif  phtml  phar  jsp  asp  aspx
```

그럼에도 blocklist 를 선택하는 상황은 분명히 있습니다.
**협업툴처럼 "어떤 파일이든 오갈 수 있어야 하는" 서비스**가 그렇습니다. 허용 목록을 운영하면 매번 "이 확장자 좀 열어달라"는 요청이 들어오고, 결국 목록이 비대해져 allowlist 의 의미가 사라집니다. 명세가 고정 확장자로 `exe`, `scr` 같은 **실행 위험군**을 지정한 것도, 특정 파일 유형만 허용하려는 게 아니라 "명백히 위험한 것만 막겠다"는 의도로 읽었습니다.

### 그래서 어떻게 보완했는가

blocklist 의 구멍을 세 층으로 메웠습니다.

| 층 | 무엇을 막는가 | 코드 |
|---|---|---|
| 파일명 정규화·검사 | 같은 확장자의 표기 변형, 파일명 위장 | `src/lib/extension/normalize.ts` |
| 내용(매직넘버) 검사 | 확장자를 아예 갈아끼운 실행 파일 | `src/lib/upload/signature.ts` |
| 저장·서빙 격리 | 위 둘이 다 뚫려도 실행되지 않게 | `src/lib/upload/storage.ts`, `next.config.ts` |

> 이 구조를 문서 첫머리에 적는 이유는, **확장자 차단 하나만 붙여놓고 "보안 처리했다"고 말하는 것이 이 기능에서 가장 흔한 실패**이기 때문입니다.

### 확장 여지
`blocked_extension.type` 에 `'ALLOWED'` 를 추가하면 같은 테이블로 allowlist 모드를 얹을 수 있습니다. 스키마를 그렇게 열어뒀지만, 명세에 없는 모드를 구현하지는 않았습니다.

---

## 2. 어디서 강제할 것인가

### 2-1. 클라이언트 검증과 서버 검증의 역할 분리

**판단**: 클라이언트 검증은 UX, 서버 검증만이 강제. **이 화면은 업로드 전에 아무것도 미리 거르지 않는다.**

**근거**:
`<input accept="...">` 도, JS 확장자 체크도 개발자도구로 지우거나 `curl` 로 우회하면 그만입니다. 그건 보안이 아니라 편의입니다.

그런데 이 과제에서는 한 걸음 더 나아가, **클라이언트에서 미리 거르지 않기로** 했습니다.
이유는 과제의 요구사항이 *"정책이 실제 업로드에서 강제되는 것"* 이기 때문입니다. 화면에서 먼저 걸러버리면 **정말로 서버가 막는 것인지 확인할 방법이 없어집니다.** 파일을 그대로 서버로 보내고, 서버의 판정 결과(사유 코드 포함)를 화면에 그대로 보여주는 편이 이 기능의 동작을 훨씬 정직하게 드러냅니다.

커스텀 확장자 입력의 `maxLength={20}`, 200개 도달 시 버튼 비활성화는 **입력 편의를 위해 남겨두되**, 서버가 같은 규칙을 독립적으로 다시 검사합니다.

**코드**: `src/components/PolicyManager.tsx` (거르지 않고 전송) → `src/app/api/upload/route.ts` → `src/lib/upload/validate.ts`

### 2-2. 강제 지점을 하나로

**판단**: `validateUpload()` 하나만이 판정을 내리고, 이를 거치지 않고 파일을 저장할 경로를 만들지 않는다.

**근거**: 검증이 여러 군데 흩어지면 나중에 추가되는 업로드 경로(예: 관리자용 일괄 업로드)에서 반드시 하나가 빠집니다. 판정 함수를 하나로 두고, 저장 함수(`storeFile`)는 그 함수의 결과를 받은 뒤에만 호출되도록 라우트를 구성했습니다.

`validateUpload()` 는 저장도 로깅도 하지 않습니다. **판정만 합니다.** 부수효과가 없으니 "정책이 A일 때 이 파일명은 어떻게 되는가"를 테스트로 고정하기 쉽습니다(`tests/validate.test.ts` 24개).

### 2-3. 3중 검증 (클라이언트 / 서버 / DB)

20자 제한을 예로 들면:

| 층 | 수단 | 역할 |
|---|---|---|
| 브라우저 | `maxLength={20}` | 21번째 글자가 안 쳐진다 (UX) |
| 서버 | `normalizeExtension()` | API 직접 호출도 막는다 (강제) |
| DB | `CHECK (extension ~ '^[a-z0-9]{1,20}$')` | 다른 경로로 INSERT 돼도 막는다 (최후) |

DB 제약을 굳이 건 이유는, **배치 스크립트나 관리 콘솔처럼 애플리케이션을 우회하는 쓰기 경로가 나중에 반드시 생기기** 때문입니다. 그때 데이터 무결성을 지켜주는 건 애플리케이션 코드가 아니라 DB 제약입니다.

### 2-4. 화면 분리 — 업로드(`/`)와 정책 설정(`/settings`)

**판단**: 파일 업로드를 메인 화면으로 두고, 차단 정책 관리는 **"차단 정책 설정" 버튼으로 진입하는 별도 페이지**로 분리한다.

**근거**: 두 기능은 **사용 빈도와 권한 수준이 전혀 다릅니다.**

| | 파일 업로드 | 차단 정책 관리 |
|---|---|---|
| 쓰는 사람 | 모든 사용자 | 관리자 |
| 빈도 | 매일, 반복적으로 | 드물게, 정책이 바뀔 때만 |
| 실수의 대가 | 파일 하나 다시 올리면 됨 | **보안 정책이 꺼진다** |

한 화면에 나란히 두면 업로드하러 온 사람이 정책 체크박스를 보게 되고, 그건 곧 **잘못 건드릴 수 있다**는 뜻입니다. 보안 설정은 "일부러 찾아 들어가야 하는" 위치에 있는 편이 안전합니다.

실무적으로 더 중요한 이유는 **인증을 붙일 때의 경계**입니다. 두 기능이 한 페이지에 있으면 라우트 단위로 권한을 나눌 수 없어, 인증 도입 시 컴포넌트 단위로 조건부 렌더를 하게 됩니다. 지금처럼 `/settings` 로 분리해 두면 **미들웨어에서 경로 하나만 막으면 끝**입니다 — 인증이 없는 현재 상태(§7-2)에서 가장 큰 미비점을 나중에 가장 싸게 메울 수 있는 구조입니다.

**업로드 화면에는 차단 "개수"만 노출한다**

업로드 화면 상단에 `현재 7개 확장자가 차단 중입니다` 만 표시하고, **어떤 확장자가 막혀 있는지는 보여주지 않습니다.**

- 업로드하는 사람이 알아야 하는 건 "정책이 켜져 있는가"입니다. 그건 업로드가 거부됐을 때 "왜 안 되지?"를 이해하는 데 필요합니다.
- 차단 목록 **전체**는 정책 관리 권한을 가진 사람이 볼 정보입니다. §7-6에서 "개별 차단 사유는 밝히되 정책 전체는 노출하지 않는다"고 정한 원칙과 같은 선입니다.

지금은 `/settings` 가 공개되어 있어 이 구분이 실질적 의미는 없지만, 인증이 붙는 순간 그대로 유효해지는 경계입니다. 구현: `countBlockedExtensions()` — 목록이 아니라 `count(*)` 만 조회합니다.

**하지 않은 것**: 업로드 화면에서 정책을 인라인으로 수정하는 기능(모달·드로어). 편해 보이지만, 위에 적은 "일부러 찾아 들어가야 한다"는 원칙과 정면으로 충돌합니다.

---

## 3. 확장자를 어떻게 볼 것인가

### 3-1. 정규화 규칙을 먼저 정의했다

명세의 *"중복 추가 방지 (예: `sh` 추가 후 다시 `sh` 추가 시 처리)"* 는, 사실 **"무엇을 같은 확장자로 볼 것인가"를 정의하라**는 요구입니다. 이걸 안 정하면 중복 판정도, 차단 판정도 성립하지 않습니다.

**정한 규칙** (`normalizeExtension()`, 순서가 중요):

| 단계 | 처리 | 막는 것 |
|---|---|---|
| 1 | NFKC 유니코드 정규화 | 전각 `ｅｘｅ` → `exe` (동형문자 우회) |
| 2 | 앞뒤 공백 제거 (비단절 공백 ` ` 포함) | `" sh "` |
| 3 | 선행 점 제거 | `.sh`, `..sh` (사용자 습관) |
| 4 | 소문자 변환 | `SH`, `Sh` |
| 5 | `^[a-z0-9]{1,20}$` 검증 | `../etc`, `tar.gz`, `실행`, `*` |

**가장 중요한 제약**: 정책 저장용 정규화(`normalizeExtension`)와 파일명 확장자 추출(`extractExtensions`)이 **반드시 같은 규칙을 써야** 합니다.
한쪽만 소문자로 접으면 정책에는 `exe` 가 저장돼 있는데 파일명에서는 `EXE` 가 나와서 매칭이 실패합니다 — **정책이 있는데 안 막히는 최악의 상태**입니다.

이걸 `tests/normalize.test.ts` 의 마지막 describe 블록(`정규화 규칙의 양방향 일치`)에서 명시적으로 검증합니다. 두 함수가 항상 같은 표준형에 도달하는지 직접 확인하는 테스트입니다.

### 3-2. 이중 확장자 — 모든 점 구간을 검사한다

**판단**: `resume.exe.pdf` 도 차단한다. (기본값 `EXTENSION_SCAN_MODE=all`)

**근거 두 가지**:

1. **서버 측 위험** — Apache `mod_mime` 계열은 중간 확장자를 핸들러로 해석해 왔습니다. `shell.php.jpg` 가 PHP 로 실행된 사고는 이 방식 때문입니다. 우리 스택(Next.js)에서는 해당하지 않지만, 파일이 다른 시스템으로 전달될 가능성을 배제할 수 없습니다.
2. **사회공학** — Windows 는 기본 설정에서 알려진 확장자를 숨깁니다. `견적서.pdf.exe` 는 사용자에게 `견적서.pdf` 로 보이고, 아이콘까지 맞춰두면 구분이 어렵습니다.

**대가를 인정합니다**: `jquery.min.js` 처럼 중간 구간 때문에 정상 파일이 걸릴 수 있습니다(과차단).
보안 컨트롤에서는 **과차단이 미차단보다 안전한 실패 방향**이라 판단해 `all` 을 기본값으로 뒀고, 운영 중 오탐이 문제가 되면 `EXTENSION_SCAN_MODE=last` 로 전환할 수 있게 설정으로 열어뒀습니다.

### 3-3. 파일명 자체가 비정상인 경우

확장자 정책과 **무관하게, 그 이전 단계에서** 끊습니다. "이 확장자가 나빠서"가 아니라 **"이 파일명이 정상적인 경로로는 만들어질 수 없어서"** 이기 때문입니다.

| 기법 | 예시 | 처리 | 사유 코드 |
|---|---|---|---|
| 후행 점 | `a.exe.` | 제거 후 검사 (Windows 가 실제로 그렇게 만든다) | `BLOCKED_EXTENSION` |
| 후행 공백 | `a.exe ` | 제거 후 검사 | `BLOCKED_EXTENSION` |
| 연속 점 | `a..exe` | 빈 구간 무시 | `BLOCKED_EXTENSION` |
| 널바이트 | `a.exe .png` | **거부** — 정상 파일명에 제어문자가 들어갈 이유가 없다 | `ILLEGAL_CONTROL_CHAR` |
| RTLO(U+202E) | 화면엔 `photo_exe.png`, 실제론 `.exe` | **거부** — 사용자를 속이려는 의도가 명백 | `ILLEGAL_BIDI_CHAR` |
| 제로폭 문자 | `a​.exe` | 거부 | `ILLEGAL_BIDI_CHAR` |
| 경로 순회 | `../../etc/cron.d/x` | 거부 | `ILLEGAL_PATH_SEPARATOR` |
| 초장문 파일명 | 4096자 | 거부 (255자 상한) | `FILENAME_TOO_LONG` |
| dotfile | `.bashrc` | 확장자 **없음**으로 판정 (오탐 방지) | — |

**RTLO 를 굳이 따로 끊은 이유**: 확장자 추출만 놓고 보면 이 파일은 어차피 `exe` 로 잡힙니다. 그래도 별도 사유로 거부하는 건 **로그의 의미** 때문입니다. `"exe 라서 막힘"` 과 `"파일명을 위장해서 막힘"` 은 완전히 다른 사건이고, 후자는 명백한 공격 시도라 대응이 달라져야 합니다.

**경로 순회를 "거부"로 처리한 이유**: 우리는 어차피 원본 파일명으로 저장 경로를 만들지 않으므로(§6-1) basename 만 취해도 안전합니다. 그럼에도 거부하는 건, 브라우저가 보내지 않는 형태의 파일명이 왔다는 것 자체가 **요청을 손으로 조립했다는 신호**라서 로그에 남길 가치가 있기 때문입니다.

---

## 4. 확장자만으로는 절대 못 막는 것

### 4-1. Content-Type 헤더를 믿지 않는다

**판단**: 클라이언트가 보낸 `Content-Type` 은 검증에 일절 사용하지 않는다.

**근거**: 그건 파일에 대한 사실이 아니라 클라이언트가 보낸 문자열입니다. `curl -F "file=@virus.exe;type=image/png"` 한 줄로 바뀝니다.

### 4-2. 매직넘버(파일 시그니처) 검증

**판단**: 파일 선두 16바이트를 읽어 실행 파일이면 **확장자와 무관하게** 차단한다.

**근거**: 이것이 §1에서 말한 "blocklist 의 구멍"에 대한 실질적인 답입니다.
`virus.exe` 를 `photo.png` 로 rename 하면 확장자 정책은 100% 통과합니다. 파일의 첫 바이트만이 공격자가 위조하기 어려운 신호입니다 — 위조하면 그 파일은 더 이상 실행되지 않으므로 공격 목적을 잃습니다.

차단 대상:

| 시그니처 | 종류 | 대응 확장자 |
|---|---|---|
| `4D 5A` (MZ) | Windows PE | exe, dll, scr, cpl, sys |
| `7F 45 4C 46` | Linux ELF | (확장자 없음이 대부분) |
| `FE ED FA CE/CF` 등 | macOS Mach-O | |
| `CA FE BA BE` | Java class | class |
| `23 21` (`#!`) | shebang 스크립트 | sh, py, pl (확장자 무관) |

**부작용을 인정합니다**: `#!` 로 시작하는 `.txt` 파일이 차단됩니다. 하지만 `#!` 로 시작하는 텍스트 파일은 실제로는 스크립트이므로, 이 컨트롤의 의도상 옳은 동작이라 판단했습니다.

**코드**: `src/lib/upload/signature.ts`, 검증 `tests/signature.test.ts`

### 4-3. 압축 파일 내부 — 의도적으로 검사하지 않음

**판단**: **하지 않는다.**

**근거**:
- zip 내부까지 검사하려면 압축을 풀어야 하고, 그 순간 **zip bomb**(42KB → 4.5PB) 이라는 더 큰 공격면이 열립니다. 크기·깊이·엔트리 수 제한을 전부 걸어야 안전해지는데, 이는 과제 범위를 크게 넘어섭니다.
- 암호가 걸린 zip 은 애초에 열 수 없습니다. 즉 완전한 방어가 불가능한 영역입니다.
- `docx`/`xlsx`/`jar`/`apk` 가 모두 `PK\x03\x04` 로 시작하므로 시그니처만으로 구분되지도 않습니다.

**대신**: 아카이브로 판별된 파일은 `upload_record.sniffed_type` 에 `archive-zip` 으로 기록해서, "내부를 모르는 파일이 들어왔다"는 사실은 남깁니다. 실제 운영이라면 이 지점에 안티바이러스(ClamAV) 스캔을 붙이는 것이 정석입니다.

### 4-4. polyglot 파일 — 막지 못함을 인정

유효한 GIF 이면서 동시에 유효한 스크립트인 파일은 시그니처 검증도 통과합니다. 이건 **내용 기반 검증의 원리적 한계**이고, 이 층에서는 막을 수 없습니다.
그래서 §6의 저장·서빙 격리가 필요합니다 — **"통과한 파일도 실행되지 않게" 만드는 것**이 마지막 답입니다.

### 4-5. SVG / HTML / Office 매크로

- **SVG, HTML** — 실행 파일은 아니지만 저장형 XSS 벡터입니다. 명세의 고정 목록에 `js` 가 들어 있는데 `svg`, `html` 이 없는 것은 일관성이 부족하다고 봅니다. 다만 **명세에 없는 확장자를 임의로 고정 목록에 추가하지는 않았고**, 대신 §6의 서빙 격리(`nosniff`, `Content-Disposition`, CSP)로 대응했습니다. 운영 판단이라면 커스텀 확장자에 추가하면 됩니다.
- **Office 매크로**(`docm`, `xlsm`) — 시그니처로 일반 문서와 구분되지 않습니다. 확장자 차단이 유일한 수단이며, 필요하면 커스텀에 추가하는 것으로 충분하다고 판단했습니다.

---

## 5. 정책 데이터 — 스키마와 동시성

### 5-1. 테이블을 하나로 둘 것인가 둘로 나눌 것인가

**판단**: **하나로 둔다** (`blocked_extension` + `type` 컬럼).

**근거**:

| | 단일 테이블 (선택) | 분리 (`fixed_*` / `custom_*`) |
|---|---|---|
| 업로드 검증 쿼리 | `WHERE is_blocked = TRUE` 한 번 | 두 테이블 UNION |
| 고정↔커스텀 중복 방지 | `UNIQUE(extension)` 하나로 해결 | 애플리케이션에서 교차 검사 필요 |
| 고정 확장자 오삭제 | `WHERE type='CUSTOM'` 조건으로 방지 | 구조적으로 불가능 (분리의 장점) |

결정적인 이유는 두 번째 줄입니다.
명세의 *"고정 확장자는 커스텀 확장자 영역에 표시되지 않음"* 은, 뒤집으면 **"커스텀에 `exe` 를 넣으려 하면 어떻게 되는가"** 라는 질문입니다.
테이블을 하나로 두면 `UNIQUE(extension)` 이 테이블 전체에 걸리므로, 이 질문의 답이 **UI 규칙이 아니라 데이터 제약으로 보장**됩니다. `curl` 로 `{"extension":"exe"}` 를 직접 POST 해도 409 가 돌아옵니다(`verify.sh` [7]).

분리 방식의 장점인 "고정 확장자 오삭제 방지"는, 삭제 쿼리에 `AND type = 'CUSTOM'` 을 넣어 대체했습니다. 권한 검사를 애플리케이션 `if` 문이 아니라 **쿼리 조건으로 내린** 것이라, id 를 조작해도 고정 확장자는 지워지지 않습니다.

### 5-2. 중복 방지의 진짜 방어선

**판단**: 애플리케이션의 `SELECT` 체크가 아니라 **`UNIQUE` 제약**이 방어선이다. 코드는 위반(SQLSTATE 23505)을 잡아 409 로 변환할 뿐이다.

**근거**: "있는지 조회하고 없으면 INSERT" 는 두 요청이 동시에 오면 반드시 뚫립니다. 두 트랜잭션이 나란히 "없음"을 확인하고 둘 다 INSERT 합니다.
검증: `tests/concurrency.integration.test.ts` — 같은 확장자를 20번 동시에 추가해도 정확히 1개만 저장됩니다.

### 5-3. 200개 제한 — `COUNT` 로는 막을 수 없다

**판단**: `pg_advisory_xact_lock` 으로 추가 연산을 직렬화한다.

**근거**: 이 부분이 이 과제에서 가장 함정이라고 생각한 지점입니다.

`SELECT count(*) < 200` 후 `INSERT` 는 199개 상태에서 두 요청이 동시에 오면 201개가 됩니다. 그리고 이건 **`SELECT ... FOR UPDATE` 로도 막히지 않습니다** — 아직 존재하지 않는 행은 잠글 수 없기 때문입니다(팬텀 리드).

가능한 선택지는 셋이었습니다.

| 방법 | 평가 |
|---|---|
| `SERIALIZABLE` 격리 수준 | 정확하지만 직렬화 실패 시 재시도 로직이 필요, 다른 트랜잭션에도 영향 |
| 테이블 `LOCK` | 확실하지만 업로드 검증(읽기)까지 막는다 |
| **advisory lock** (선택) | 추가 연산만 직렬화. 읽기는 전혀 막지 않음. 트랜잭션 종료 시 자동 해제 |

`INSERT` 트리거로 카운트를 강제하는 방법도 있었지만, 비즈니스 규칙을 트리거에 숨기면 코드만 읽어서는 동작을 알 수 없게 되어 채택하지 않았습니다.

**검증**: 199개를 채운 뒤 10개를 동시에 던져서 **정확히 1개만 성공하고 9개가 `LIMIT_EXCEEDED`** 로 거부되는 것을 확인했습니다. advisory lock 이 없으면 여기서 209개가 됩니다.

### 5-4. 삭제 — hard delete + 감사 로그

**판단**: 커스텀 확장자는 **hard delete**, 대신 삭제 사실을 `extension_policy_audit` 에 남긴다.

**근거**:
- soft delete(`deleted_at`)를 쓰면 `UNIQUE(extension)` 이 깨집니다. `sh` 삭제 후 재추가 시 유령 행과 충돌하므로, 부분 유니크 인덱스나 복합 키로 우회해야 하는데 **200개짜리 정책 테이블에 그만한 복잡도를 들일 이유가 없습니다.**
- 정작 필요한 건 "지워진 행"이 아니라 **"언제 누가 지웠는가"** 입니다. 그건 별도 감사 테이블이 더 정확하게 답합니다.
- `actor` 컬럼은 인증이 없어 현재 `'anonymous'` 고정이지만, 컬럼을 미리 확보해 인증 도입 시 스키마 변경 없이 채울 수 있게 했습니다.

### 5-5. 정책 캐시

**판단**: 5초 TTL + **정책 변경 시 명시적 무효화**.

**근거**: 보안 정책 캐시에는 **방향성**이 있습니다.

- "차단해야 하는데 캐시가 낡아서 통과시킨다" → **사고**
- "해제했는데 캐시가 낡아서 몇 초 더 막는다" → 불편

두 실패의 무게가 다르므로, 무효화를 1차 수단으로 삼고 TTL 은 **무효화를 놓쳤을 때의 안전망**으로만 씁니다.
서버리스에서는 함수 인스턴스마다 캐시가 따로 존재해 한 인스턴스의 무효화가 다른 인스턴스에 전파되지 않으므로, TTL 이 반드시 필요합니다. Redis 같은 공유 캐시는 최대 207행 규모에 과합니다.

`verify.sh` [10] 에서 정책 해제 후 6초(TTL+1) 대기 후 통과를 확인해, 무효화가 실패해도 TTL 로 회복되는지 함께 검증합니다.

### 5-6. 인덱스

```sql
CREATE INDEX ix_blocked_extension_active
    ON blocked_extension (extension) WHERE is_blocked = TRUE;
```

업로드 검증 쿼리는 항상 `WHERE is_blocked = TRUE` 만 봅니다. 부분 인덱스로 차단 중인 행만 색인하면 해제된 정책은 인덱스에 들어가지 않습니다.

**솔직히 말하면 이 규모(최대 207행)에서 실측 이득은 거의 없습니다.** 풀스캔이 더 빠를 수도 있습니다. 그럼에도 건 이유는, 정책 테이블이 커져도(멀티테넌시로 확장되는 등) 실행 계획이 바뀌지 않게 하기 위함입니다. 인덱스를 "왜 걸었는지" 설명할 수 없다면 걸지 않는 편이 낫다고 생각해서, 이 근거를 스키마 파일 주석에도 남겼습니다.

---

## 6. 저장과 서빙 — 뚫려도 피해가 없게

§4-4에서 인정했듯 내용 기반 검증도 완전하지 않습니다. 그래서 **"검증을 통과한 파일도 실행되지 않게"** 만드는 층이 필요합니다.

### 6-1. 저장 규칙

| 규칙 | 이유 |
|---|---|
| 파일명을 **서버가 UUID 로 정한다** | 사용자가 준 이름으로 경로를 만드는 순간 `../../etc/cron.d/x` 가 가능해진다. 원본 이름은 DB 컬럼에 문자열로만 보관 |
| **확장자를 붙이지 않는다** | 디스크에 `a3f2…` 로만 저장. 파일명에 실행 확장자가 없으면 웹서버가 핸들러를 물릴 여지 자체가 없다 |
| **웹 루트(`public/`) 바깥** | `public/` 에 두면 Next.js 가 그대로 정적 서빙한다. 업로드한 HTML 이 같은 오리진에서 실행되면 저장형 XSS |
| **권한 `0600`** | 실행 비트를 애초에 주지 않는다 |
| 경로 탈출 재확인 | 저장명이 UUID 라 불필요하지만, 생성 방식이 바뀌어도 안전하도록 `resolve` 후 접두사를 확인한다 |

**코드**: `src/lib/upload/storage.ts`

### 6-2. 서빙 헤더

`next.config.ts` 에서 전역 적용:

- `X-Content-Type-Options: nosniff` — 브라우저가 Content-Type 을 무시하고 내용으로 타입을 추측하는 동작을 끕니다. 업로드 파일이 `text/plain` 으로 내려가도 HTML 로 해석되는 사고를 막습니다.
- `Content-Security-Policy` — 저장형 XSS 가 성립하더라도 외부로 데이터를 보내기 어렵게 합니다.
- `X-Frame-Options: DENY`, `frame-ancestors 'none'` — 클릭재킹.

**하지 않은 것**: 파일 다운로드 엔드포인트를 만들지 않았습니다(명세 범위 밖). 만들었다면 `Content-Disposition: attachment` 강제와 **별도 도메인/버킷 서빙**(쿠키·세션 격리)이 필수입니다.

### 6-3. 서버리스에서의 저장 — 명시적으로 버린다

**판단**: Vercel 배포 시 기본값은 `STORAGE_DRIVER=none` — 검증은 정상 수행하되 **파일 본문을 보관하지 않고, 그 사실을 응답에 명시**한다.

**근거**: 서버리스는 파일시스템이 휘발됩니다. 로컬 디스크에 쓰면 다음 요청에서 사라집니다.
**"저장된 것처럼 보이지만 실제로는 없는" 상태가 가장 나쁩니다.** 조용히 실패하느니 명시적으로 버리고, 응답의 `persisted: false` 와 화면 메시지로 그 사실을 드러냈습니다.

실제 운영이라면 S3/R2 presigned URL 로 직접 업로드하고 서버는 메타데이터만 다루는 구조가 맞습니다. 다만 그 경우 **검증 시점이 업로드 이후로 밀리므로**, presigned URL 발급 단계에서 파일명 정책을 먼저 검사하고 업로드 완료 훅에서 내용을 재검증하는 2단 구조가 필요합니다. 이건 과제 범위를 넘어 구현하지 않았습니다.

---

## 7. 운영 관점

### 7-1. 정책 변경의 소급 적용

**판단**: **소급 적용하지 않는다.**

**근거**: `exe` 를 새로 차단해도 이미 업로드된 `exe` 파일은 그대로 둡니다.
정책은 "지금부터 들어오는 파일"에 대한 게이트입니다. 소급 적용하면 이미 사용자가 공유·참조하고 있는 파일이 예고 없이 사라지고, 그 영향 범위를 예측할 수 없습니다.
실제 운영이라면 "정책 변경 시 기존 파일 영향도 리포트"를 별도 기능으로 제공하고, 삭제 여부는 사람이 결정하게 하는 것이 맞다고 봅니다.

### 7-2. 권한 — 인정하는 미구현

**판단**: 인증/인가를 구현하지 않았다. **다만 이것이 이 애플리케이션의 가장 큰 미비점이다.**

**근거**: 과제가 "누구나 접속 가능한 URL"을 요구하므로 로그인을 붙이면 채점 자체가 불편해집니다. 그래서 명시적으로 제외했습니다.

**그러나** 현재 상태는 **누구나 차단 정책을 해제할 수 있는** 상태입니다. 정책 관리 화면은 본질적으로 관리자 기능이며, 실제 서비스라면 다음이 필요합니다.

- 정책 관리 API 에 관리자 권한 검사
- `extension_policy_audit.actor` 에 실제 사용자 식별자 기록 (컬럼은 이미 준비됨)
- 정책 변경에 대한 알림 (보안 설정 해제는 조용히 일어나면 안 됨)

**나중에 싸게 메울 수 있게 준비해 둔 것** (§2-4):
정책 관리를 `/settings` 라우트로 분리해 뒀기 때문에, 인증 도입 시 **미들웨어에서 `/settings` 와 정책 CRUD API 경로만 막으면** 됩니다. 컴포넌트마다 조건부 렌더를 흩뿌릴 필요가 없습니다. 감사 로그의 `actor` 컬럼도 이미 있어 스키마 변경 없이 채울 수 있습니다.
설정 페이지 상단에는 이 상태를 숨기지 않고 안내 배너로 명시했습니다 — 데모용으로 인증을 뺀 것과, 인증을 고려하지 않은 것은 다릅니다.

### 7-3. 감사 로그

두 종류를 남깁니다.

- `extension_policy_audit` — 정책 변경 이력. 커스텀 확장자를 hard delete 하므로 삭제 사실은 여기에만 남습니다.
- `upload_record` — 업로드 시도 기록. **성공보다 차단된 시도가 중요합니다.** 같은 출처에서 `a.exe` → `a.EXE` → `a.exe.png` → `a.exe .png` 순으로 들어오면 그건 우회 공격이고, 이 로그가 그걸 탐지할 유일한 신호입니다.

**하지 않은 것**: IP/세션 기록. 개인정보 성격이 있어 보관 정책 없이 남기면 안 된다고 판단했습니다. 실제 운영이라면 보관 기간을 정하고 남기는 것이 맞습니다.

### 7-4. 원자성 — 파일과 DB의 불일치

**판단**: **DB 기록 실패가 업로드를 실패시키지 않는다.** 대신 콘솔에 남긴다.

**근거**: 감사 로그는 부가 기능이고 사용자 요청은 본 기능입니다. 로깅 실패로 업로드 전체를 되돌리는 건 과합니다.
반대 방향(파일은 저장됐는데 DB 기록이 실패)에서는 **고아 파일**이 남습니다. 실제 운영이라면 주기적 정리 배치가 필요하고, 현재는 `STORAGE_DRIVER=none` 이 기본이라 이 문제가 발생하지 않는 상태입니다. 이 트레이드오프를 인지하고 남겨둔 것이지, 놓친 것은 아닙니다.

### 7-5. 크기 제한과 DoS

- **파일 크기 4MB** — 무제한이면 스토리지·메모리 고갈로 가는 가장 싼 DoS 경로가 열립니다. Vercel Serverless 의 요청 본문 상한(4.5MB)보다 작게 잡아, 플랫폼이 먼저 끊어서 원인 불명 에러가 나는 상황을 피했습니다.
- **크기 검사를 바이트를 메모리에 올리기 전에** 수행합니다(`file.size` 먼저 확인). 여기서 안 끊으면 4MB 제한이 있어도 400MB 짜리를 일단 다 읽고 나서 거절하게 됩니다.
- **하지 않은 것**: Rate limiting. 무료 호스팅 환경에서 의미 있게 구현하기 어렵고(인스턴스별 메모리 카운터는 무의미), 실제로는 Cloudflare/WAF 계층에서 처리하는 것이 맞다고 판단했습니다.

### 7-6. 에러 메시지 — 얼마나 자세히 말할 것인가

**판단**: **차단 사유를 명확히 밝힌다.** 다만 정책 전체를 노출하지는 않는다.

**근거**: "업로드할 수 없습니다"만 말하면 사용자는 고칠 방법이 없습니다.
`.exe 확장자는 차단 정책에 의해 업로드할 수 없습니다` 처럼 **어떤 확장자 때문인지**는 밝히되, 전체 차단 목록을 응답에 담지는 않습니다. 공격자가 이미 그 확장자를 시도해서 알아낸 정보를 확인해 주는 것과, 목록 전체를 넘겨주는 것은 다른 문제입니다.

내부 에러(스택 트레이스, SQL, 커넥션 문자열)는 절대 클라이언트로 흘리지 않고 `INTERNAL_ERROR` 로 통일합니다.

또한 사유를 **코드**(`BLOCKED_EXTENSION`, `EXECUTABLE_SIGNATURE`)와 **메시지**로 분리했습니다. 코드는 집계·모니터링용, 메시지는 사람용입니다. 다국어 대응 시에도 코드만 있으면 됩니다.

### 7-7. UX 예외 처리

| 상황 | 처리 |
|---|---|
| 200개 도달 | 입력창·버튼 비활성화 + 카운터 경고색 + 서버 409 |
| 20자 초과 | `maxLength` 로 입력 차단 + 서버 400 |
| 빈 입력 | 버튼 비활성화 |
| 중복 | 서버 사유를 그대로 표시 (`'sh' 은(는) 이미 등록된 확장자입니다.`) |
| 통신 실패 | 낙관적 업데이트를 **롤백**하고 오류 표시 |
| 처리 중 | 개별 항목 단위 비활성화(`busyIds`) — 연타로 중복 요청이 나가지 않게 |

**낙관적 업데이트에 롤백을 반드시 붙인 이유**: 체크박스는 즉시 반응해야 하지만, 롤백 없는 낙관적 업데이트는 **"저장된 줄 알았는데 아니었다"** 를 만듭니다. 보안 설정에서 이건 치명적입니다.

### 7-8. 접근성

`aria-label`(파일 입력, 삭제 버튼), `<label>` 로 감싼 체크박스(레이블 클릭으로 토글), 다크/라이트 모드 대응, 포커스 아웃라인 유지. 다만 스크린리더 실사용 테스트는 하지 못했습니다.

---

## 8. 의도적으로 하지 않은 것 (요약)

| 항목 | 이유 |
|---|---|
| 인증/인가 | "누구나 접속 가능" 요구와 충돌. **가장 큰 미비점으로 인정** (§7-2) |
| 압축 파일 내부 검사 | zip bomb 이라는 더 큰 공격면을 연다 (§4-3) |
| 안티바이러스 스캔 | 무료 호스팅에서 ClamAV 운영 불가. 실제 운영이라면 필수 |
| Rate limiting | 서버리스에서 인스턴스별 카운터는 무의미. WAF 계층의 몫 (§7-5) |
| 정책 변경 소급 적용 | 예측 불가능한 영향 범위 (§7-1) |
| soft delete | `UNIQUE` 제약과 충돌. 감사 로그가 더 정확한 답 (§5-4) |
| 파일 다운로드 기능 | 명세 범위 밖. 만든다면 별도 도메인 서빙이 필수 (§6-2) |
| allowlist 모드 | 명세에 없음. 스키마는 확장 가능하게 열어둠 (§1) |
| IP/세션 로깅 | 보관 정책 없이 개인정보를 남기지 않음 (§7-3) |
| CSP nonce 적용 | Next.js 인라인 스크립트 때문에 `unsafe-inline` 필요. nonce 로 좁힐 수 있으나 범위 초과 |

---

## 9. 남아 있는 위험

정직하게 적습니다. 이 구현으로도 다음은 막지 못합니다.

1. **polyglot 파일** — 유효한 이미지이면서 유효한 스크립트인 파일. 시그니처 검증을 통과합니다. §6의 저장 격리에 의존합니다.
2. **아카이브 내부의 실행 파일** — §4-3의 판단에 따라 검사하지 않습니다.
3. **blocklist 에 없는 위험 확장자** — `msi`, `vbs`, `ps1`, `jar`, `hta`, `lnk` 등. 이것이 §1에서 말한 blocklist 의 원리적 한계입니다.
4. **정책 관리 화면이 공개되어 있음** — 누구나 차단을 해제할 수 있습니다. §7-2.
5. **매크로가 포함된 Office 문서** — 시그니처로 구분되지 않습니다.
6. **DB 기록 실패 시 고아 파일** — `STORAGE_DRIVER=local` 사용 시에 한해 발생 가능. §7-4.

이 목록을 남기는 이유는, **막지 못하는 것을 알고 있는 상태와 모르는 상태는 전혀 다르기** 때문입니다. 위 항목들은 각각 어느 층에서 보완해야 하는지가 이 문서에 함께 적혀 있습니다.
